# ArchUnit 1.x fails a should() that matches zero elements by default. This slice is a
# PARTIAL tree (one verb, empty layers), so a defined-but-empty rule legitimately matches
# nothing — tolerate it (investigator brief, AC8).
archunit.fail.on.empty.should=false
